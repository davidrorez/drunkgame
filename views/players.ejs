<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jugadores | Drunk Game</title>
    <%- include('partials/head')%>
</head>

<body>

    <script> "https://code.jquery.com/jquery-3.6.1.min.js" </script>
    <%- include ('partials/navigation') %>

        <header class="container vh-100 d-flex justify-content-center align-items-center">
            <div class="d-flex flex-column justify-content-center align-items-center ">
                <img src="/resources/img/drunk.png" alt="" width="200" height="175" class="img-fluid mb-3">
                <form action="/players" method="POST">
                    <div class="input-group mb-4">
                        <input type="text" name=playerName required class="form-control" placeholder="Nombre jugador"
                            aria-describedby="basic-addon2">
                        <div class="input-group-append">
                            <button class="btn btn-dark " type="submit">Agregar</button>
                        </div>
                    </div>
                </form>
                <% if (nameTest.length> 0) { %>
                    <h5>&#128293; <b> Jugadores </b> &#128293;</h5>
                        <h5>
                             <%= nameTest.join(", ") %>
                        </h5>
                            <%} %>
                                <% if (nameTest.length == 6) { %>
                                    <script> Swal.fire('¡Capacidad máxima de jugadores!'); </script>
                                    <%} %>
                                        <% if (nameTest.length == 0) { %>
                                            <a class="btn btn-dark w-25">Jugar</a>
                                            <% } else { %>
                                                <a href="/game" class="btn btn-dark w-25">Jugar</a>
                                                <%} %>

            </div>
        </header>
        </div>
        <%- include ('partials/footer') %>
</body>

</html>
